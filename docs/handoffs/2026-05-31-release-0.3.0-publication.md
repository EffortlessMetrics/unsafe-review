# 2026-05-31 - release 0.3.0 publication receipt

Scope: record manual publication and post-publish verification for
`unsafe-review` `0.3.0`.

## Source

- Source repository: <https://github.com/EffortlessMetrics/unsafe-review>
- Source commit: `98585f3d0a155031a078c112844df4f2f5d3439c`
- Preparation PR: <https://github.com/EffortlessMetrics/unsafe-review/pull/503>
- Tag: `v0.3.0`
- GitHub Release: <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.0>

## Published crates

| Crate | Version | crates.io | docs.rs |
|---|---|---|---|
| `unsafe-review-core` | `0.3.0` | <https://crates.io/crates/unsafe-review-core/0.3.0> | <https://docs.rs/unsafe-review-core/0.3.0/unsafe_review_core/> |
| `unsafe-review-cli` | `0.3.0` | <https://crates.io/crates/unsafe-review-cli/0.3.0> | <https://docs.rs/unsafe-review-cli/0.3.0/unsafe_review_cli/> |
| `unsafe-review` | `0.3.0` | <https://crates.io/crates/unsafe-review/0.3.0> | <https://docs.rs/unsafe-review/0.3.0/unsafe_review/> |

## Publish order

Published manually in dependency order:

```bash
rtk cargo publish -p unsafe-review-core
rtk cargo search unsafe-review-core --limit 10

rtk cargo publish -p unsafe-review-cli --dry-run
rtk cargo publish -p unsafe-review-cli
rtk cargo search unsafe-review-cli --limit 10

rtk cargo publish -p unsafe-review --dry-run
rtk cargo publish -p unsafe-review
rtk cargo search unsafe-review --limit 10
```

Observed registry state:

- `cargo search unsafe-review --limit 10` reported all three crates at `0.3.0`.
- `cargo info unsafe-review-core@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.
- `cargo info unsafe-review-cli@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.
- `cargo info unsafe-review@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.

## Docs and release

- `curl -I https://docs.rs/unsafe-review-core/0.3.0/unsafe_review_core/`
  returned `HTTP/1.1 200`.
- `curl -I https://docs.rs/unsafe-review-cli/0.3.0/unsafe_review_cli/`
  returned `HTTP/1.1 200`.
- `curl -I https://docs.rs/unsafe-review/0.3.0/unsafe_review/` returned
  `HTTP/1.1 200`.
- `gh release view v0.3.0 --repo EffortlessMetrics/unsafe-review` reported a
  non-draft, non-prerelease GitHub Release published at
  `2026-05-31T09:36:52Z`.

## Published install smoke

Installed from crates.io into an isolated root:

```bash
rtk cargo install unsafe-review --version 0.3.0 --locked --root target/install-published-0.3.0
```

Observed:

```text
target/install-published-0.3.0/bin/unsafe-review.exe v0.3.0
```

Version smoke:

```bash
rtk target/install-published-0.3.0/bin/unsafe-review.exe --version
```

Observed:

```text
unsafe-review 0.3.0
```

Doctor smoke:

```bash
rtk target/install-published-0.3.0/bin/unsafe-review.exe doctor
```

Observed: git, repo, `origin/main`, cargo metadata, artifact directory,
witness tool signals, advisory policy, and trust-boundary output were present.
Missing witness tools were informational.

## First-pr artifact smoke

Command:

```bash
rtk target/install-published-0.3.0/bin/unsafe-review.exe first-pr --root fixtures/raw_pointer_alignment --diff fixtures/raw_pointer_alignment/change.diff --out-dir target/unsafe-review-published-0.3.0-smoke
rtk cargo run --locked -p xtask -- check-first-pr-artifacts target/unsafe-review-published-0.3.0-smoke
```

Observed:

- `first-pr` wrote one advisory PR bundle with one ReviewCard.
- `check-first-pr-artifacts` returned ok for
  `target/unsafe-review-published-0.3.0-smoke`.
- The published bundle included:

```text
review-kit.json
cards.json
pr-summary.md
github-summary.md
cards.sarif
comment-plan.json
witness-plan.md
receipt-audit.md
lsp.json
repair-queue.json
```

Top smoke card:

```text
UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1
```

## Explain, context, support, and badge smoke

Commands:

```bash
rtk target/install-published-0.3.0/bin/unsafe-review.exe explain --root fixtures/raw_pointer_alignment UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1
rtk target/install-published-0.3.0/bin/unsafe-review.exe context --root fixtures/raw_pointer_alignment UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1 --json
rtk target/install-published-0.3.0/bin/unsafe-review.exe support
rtk target/install-published-0.3.0/bin/unsafe-review.exe badges --out target/published-0.3.0-badges
```

Observed:

- `explain` rendered the top card with required safety conditions, evidence
  found, evidence missing, witness routes, non-resolution examples, and trust
  boundary.
- `context --json` rendered a bounded card packet with repair queue buckets
  `repairable_by_guard` and `requires_witness_receipt`, allowed repairs,
  do-not-do rules, stop conditions, and trust boundary.
- `support` reported the first-pr bundle, receipt audit, saved LSP JSON, repair
  queue, and the advisory/deferred posture.
- Badge smoke wrote numeric endpoint messages:
  - `target/published-0.3.0-badges/unsafe-review.json`: `message = "234"`
  - `target/published-0.3.0-badges/unsafe-review-plus.json`:
    `message = "227"`

## Trust boundary

`unsafe-review` `0.3.0` reports static unsafe-review evidence. It does not
prove memory safety, UB-free status, Miri-clean status, site execution, witness
success, calibrated precision/recall, or policy readiness.

It does not execute witnesses, post comments, edit source, or enforce blocking
policy by default.

## Known limits

- All current public surfaces remain experimental advisory surfaces.
- The release is not a calibrated analyzer or policy gate.
- Bun investigation is a post-release use case; any Bun candidate still requires
  manual proof of a safe API path, local prevention, and a focused regression or
  witness appropriate to that finding.

## Next sync

Mirror this publication receipt back to `unsafe-review-swarm` before routine
swarm development resumes.
