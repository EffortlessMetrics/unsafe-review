diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,0 +1,5 @@
+pub fn checked_len(bytes: &[u8]) -> usize {
+    bytes.len()
+}
+
+pub fn double(value: usize) -> usize { value.saturating_mul(2) }
