diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,7 +1,7 @@
 pub fn data_end(ctrl: Ptr) -> Ptr {
-    unsafe { make_non_null(ctrl.as_ptr().cast()) }
+    ctrl.cast()
 }
 
 pub unsafe fn data_start(ptr: Ptr) -> Ptr {
     ptr
 }
